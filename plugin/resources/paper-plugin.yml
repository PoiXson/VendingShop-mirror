name: ${project.name}
main: com.poixson.vendingshop.VendingShopPlugin
version: '${project.version}'
api-version: '${bukkit-short-version}'
depends:
  - pxnPluginLib
  - Vault
authors: lorenzop
website: ${project.url}
description: ${project.description}
permissions:
    vending.*:
        default: op
        description: Gives access to all vending commands
        children:
